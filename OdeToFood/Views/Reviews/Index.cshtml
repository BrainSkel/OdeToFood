@model Restaurant

@{
    ViewData["Title"] = "Index";
}

<h1>Reviews for @Model.Name</h1>
<partial name="_Reviews" model="@Model.Reviews" />

<p>
    <a asp-action="Create" asp-route-restaurantId="@Model.Id">Create New</a>
</p>